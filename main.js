let myLibrary = [];

function Book(title, author, numOfPages, isRead) {
  this.title = title;
  this.author = title;
  this.numOfPages = numOfPages;
  this.isRead = isRead;
}

function addBookToLibrary() {
  // do stuff here
}
